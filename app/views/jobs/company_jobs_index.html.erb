<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <header class="header">
      <div>All Jobs for <%= @company.name %></div>
    </header>
    <section class="whole">
      <article class="main">
        <table class="table table-striped container-fluid">
          <thead>
            <tr class="table-primary">
              <th style="width: 30%">Job Title</th>
              <th style="width: 30%"><%= link_to 'Add a New Job', new_company_job_path(@company) %></th>
              <th style="width: 10%">City</th>
              <th style="width: 20%">Level of Interest</th>
              <th style="width: 10%">Category</th>
            </tr>
          </thead>
          <tbody>
            <% @jobs.each do |job| %>
            <tr>
              <td style="width: 30%"><%= link_to job.title, job_path(job) %></td>
              <td style="width: 30%"></td>
              <td style="width: 10%"><%= job.city %></td>
              <td style="width: 20%"><%= job.level_of_interest %></td>
              <td style="width: 10%"><%= link_to job.category.title, category_path(job.category) %></td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </article>
    </section>
  </body>
</html>
