<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <header class="header">
      <div>All Jobs for <%= @company.name %></div>
    </header>
    <section class="whole">
      <article class="main">
        <table class="table table-striped container-fluid">
          <thead>
            <tr class="table-primary">
              <th style="width: 40%">Job Title</th>
              <th style="width: 20%">Company Name</th>
              <th style="width: 20%">Job Id</th>
              <th style="width: 20%"><%= link_to 'Add a New Company', new_company_path %></th>
            </tr>
          </thead>
          <tbody>
            <% @jobs.each_with_index do |job, index| %>
            <tr>
              <td style="width: 40%"><%= link_to job.title, company_job_path(@company, job) %></td>
              <td style="width: 20%"><%= job.company.name %></td>
              <td style="width: 20%"><%= job.id %></td>
              <td style="width: 20%" align='center'></td>
            </tr>
            <% end %>
          </tbody>
        </table>
        <div class="colored"><%= link_to '<< Back to Companies', companies_path %><div>
      </article>
    </section>
  </body>
</html>
