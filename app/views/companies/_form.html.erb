<%= form_for @company do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit class: "btn btn-primary" %>
  <%= link_to 'Cancel', companies_path, class: 'btn btn-danger', id: 'category-cancel-button'%>
<% end %>
