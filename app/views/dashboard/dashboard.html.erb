<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <header class="header">
      <div>Job Tracker Dashboard</div>
    </header>
      <div class="space-30"></div>
      <div class="container-fluid">
        <div class="row">
          <div class="col-2"></div>
          <div class="col-3 colored padded column-center">
            <h3>Jobs by Interest</h3>
            <article class="jobs-by-interest">
              <% @interest_levels.each do |stars, jobs| %>
                <div><%= '*' * stars %><%= " (#{jobs} jobs)" %></div>
                <br/>
              <% end %>
            </article>
          </div>
          <div class="col-2"></div>
          <div class="col-3 colored padded column-center">
            <h3>Top Companies by Interest</h3>
            <article class="">
              <% @top_3_companies.each do |company| %>
                <br>
                <p><%= company.name %> (<%= company.avg_level_of_interest.round(1) %> stars)</p>
              <% end %>
            </article>
          </div>
          <div class="col-1"></div>
        </div>
      </div>
      <div class="space-30"></div>
      <div class="col flex-center">
        <article class="middle-width colored padded column-center">
          <h3>Jobs by Location</h3>
          <% @jobs_by_city.each do |city, jobs| %>
            <li><%= link_to "#{city} jobs", jobs_path(location: city) %> (<%= jobs %> jobs)</li>
          <% end %>
        </article>
      </div>
  </body>
</html>
